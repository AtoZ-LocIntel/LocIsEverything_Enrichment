// Use the plugin's own type definitions (it already augments `leaflet` with `maplibreGL` + `getMaplibreMap()`).
// Importing the module here ensures TypeScript includes its `.d.ts` augmentation under bundler resolution.
import '@maplibre/maplibre-gl-leaflet';


